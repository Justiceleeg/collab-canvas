// TODO: PR #4 - Basic Canvas with Pan & Zoom
// Konva Stage wrapper component
// - Mark as 'use client' - Konva requires DOM
// - Konva Stage setup
// - Pan and zoom controls (mouse wheel + drag)
// - Viewport state management

export default function Stage() {
  return (
    <div>
      {/* TODO: Implement Stage component with Konva */}
    </div>
  );
}

